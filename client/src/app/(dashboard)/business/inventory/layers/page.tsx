import React from 'react';
import InventoryLayersDashboard from '@/features/inventory/InventoryLayersDashboard';

export const metadata = {
    title: 'Inventory Cost Layers | FastPOS',
};

export default function LayersPage() {
    return <InventoryLayersDashboard />;
}
